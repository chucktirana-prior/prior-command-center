// Point this at your running Prior Command Center backend.
// For local dev: use your Mac's LAN IP so the phone/simulator can reach it.
// e.g. "http://192.168.1.100:3001"
export const API_BASE_URL = 'http://localhost:3001';
